# 设计报告

> 2026 全国大学生嵌入式芯片与系统设计竞赛 · FPGA 创新设计赛道
> **自主选题赛道（初级组）**
>
> 本机环境：Vivado / Vitis **2025.2** · 器件 `xc7z020clg400-1`（PYNQ-Z2）

| 章节 | 内容 |
|---|---|
| ① | [选题背景与创新点](#一选题背景与创新点) |
| ② | [设计原理与功能框图](#二设计原理与功能框图) |
| ③ | [软硬件划分依据与接口设计](#三软硬件划分依据与接口设计) |
| ④ | [优化过程](#四优化过程) |
| ⑤ | [大模型协作记录](llm-collaboration.md) |
| ⑥ | [技能包的提炼过程](#六技能包的提炼过程) |
| ⑦ | [复现说明](#七复现说明) |

---

## 一、选题背景与创新点

### 1.1 选题：嵌入式手势识别

摄像头实时采集 → PL 侧预处理 → 得到手部轮廓 → 神经网络分类。

**解决的问题**：手势交互需要**低延迟**（跟随动作，不是按一下等一秒）。
这正好落在 FPGA 的强项上 —— **确定性的低延迟流水线**，
而不是 CPU 的"算得快但抖动大"。

### 1.2 面向的竞赛方向

赛题 §3.3.2 的推荐方向里，**"视觉识别"与"图像预处理加速"**都在列。
本作品的定位是**完整的 FPGA 作品**：从摄像头采集一路做到分类结果。

### 1.3 创新点

#### ① 异构划分沿"数据量 × 操作次数"这条线切，而不是沿"难易"切

这是本作品**最核心的设计判断**（详见 §三）。

常见做法是把 CNN 也塞进 PL —— 本作品**明确否掉**，理由是可量化的：

| 约束 | XC7Z020 实际值 | 微型 CNN 全并行的需求 | 判断 |
|---|---|---|---|
| DSP48E1 | **220** | `Conv(1→8, 3×3)` 单层 = 72 个乘法器 | **单层就吃掉 1/3** |
| LUT | 53,200 | 多层流水 + 权重 ROM 寻址 | 会超 50% |
| 动态功耗 | 现有设计约 1.7 W | 全并行会顶到板卡供电/散热上限 | 有风险 |
| 开发工期 | — | 自研 line buffer + 权重调度 + 定标 | **以月计** |
| **替代方案** | — | **Cortex-A9 (650 MHz) 跑定点推理** | **10–30 ms/帧** |

**关键判断**：目标 30 FPS，PS 侧 30 ms/帧**完全够用** ——
因为**瓶颈根本不在推理，在摄像头采集与预处理**。

> **答辩时会被问，所以提前写清**：不是"PL 做不到"，
> 而是**"用最贵的资源解最容易的问题"**。

#### ② 用"接口契约"解耦分工，而不是用接口定义解耦

`DDR 里 96×96 uint8 灰度 buffer` 是**唯一**的跨侧边界（详见 §三）。
两侧各自把它当黑盒 —— **这让 CNN 侧可以在 PC 上独立开发调试**，
不受板子是否到货影响。

#### ③ 把"验证"当成设计的一部分，而不是收尾动作

本作品对每一层都设了**独立的验证手段**（见 [§四](#四优化过程) 与 `../skill/self-correction/`），
且明确区分"已验证 / 未验证"。这个习惯直接产出了
[`../skill/`](../skill/README.md) 里那套可复用的东西。

### 1.4 前人做过什么

#### 直接复用的工程资产

| 来源 | 复用了什么 | 许可 |
|---|---|---|
| **Sobel 加速器**（本仓库 `legacy/sobel/`） | **行缓存骨架**（3 行 BRAM + 3 级列移位寄存器） | 本项目自有 |
| **PYNQ**（AMD 官方框架） | Overlay / MMIO / DMA 编程模型 | BSD-3 |
| [`xupsh/Pynq-CV-OV5640`](https://github.com/xupsh/Pynq-CV-OV5640) | OV5640 配置表来源、接线参考 | BSD-3 |
| [`ambitously/zynq7020-image-enhancement`](https://github.com/ambitously/zynq7020-image-enhancement) | 正点原子 Zynq7020 + OV5640 的采集通路参考 | **无 LICENSE** |

> ⚠ `ambitously` 那个仓库**没有 LICENSE**，本项目**只参考其思路与接线，
> 未复制其代码**。若后续要复用代码，须先联系作者。

#### 学术前人工作

> **核验说明**：以下文献均通过出版方页面或 DOI 核验过存在性。
> **未核验通过的不列入**。

**① 与本作品最接近：手势识别 + PYNQ-Z2 + ResNet**

> Xiaoran Li, et al. *Hardware accelerator for high accuracy sign language
> recognition with residual network based on FPGAs.*
> **IEICE Electronics Express**, Vol.21, No.4, 2024.
> DOI: [10.1587/elex.21.20230579](https://doi.org/10.1587/elex.21.20230579)

- **平台**：**PYNQ-Z2**（与本作品同板）
- **场景**：**手势/手语识别**（与本作品同场景）
- **方法**：层间融合 + 8 位动态量化；通道并行 + 紧流水
- **结果**：硬件推理精度 **98.87%**，总功耗 **2.136 W**

**对本作品的意义**：它证明"**PYNQ-Z2 上跑手势识别 CNN 可行**"，
并给出功耗量级（2.1 W）。本作品 §1.3① 里"动态功耗约 1.7 W"的
判断与之同量级 —— 说明**把 CNN 放 PL 会顶到供电/散热上限**的顾虑是合理的。

**② 最直接的方法论参照：PL vs 同一块板的 PS，量化加速比**

> Kaijie Wei, Koki Honda, Hideharu Amano.
> *An implementation methodology for Neural Network on a Low-end FPGA Board.*
> **CANDAR 2020**, pp. 228-234.
> DOI: [10.1109/CANDAR51075.2020.00039](https://doi.org/10.1109/CANDAR51075.2020.00039)

- **平台**：**PYNQ-Z1**
- **方法**：算法级降尺度 + HLS 硬件优化
- **结果**：**相比同一块板上的 PYNQ ARM CPU，加速 22×**；
  能效比 Xeon E5-2667 高 **3×**

**对本作品的意义**：**它做的正是本项目 §4.4 待补的那个对比** ——
PL 相对**同一块板的 PS** 的加速比。**本作品应做同样的测量**（尚未做，见 §4.4），
可与该文的 22× 对照。**这是本作品基线工作的直接方法论依据。**

**③ 同板同摄像头、软硬件协同的参照**

> Gang Wu, Jinglei Yang, Hao Yang.
> *Real-time low-power binocular stereo vision based on FPGA.*
> **Journal of Real-Time Image Processing**, 19(1): 29-39, 2022.
> DOI: [10.1007/s11554-021-01158-z](https://doi.org/10.1007/s11554-021-01158-z)

- **平台**：**PYNQ-Z2 @ 100 MHz**（与本作品同板同频）
- **方法**：**软硬件协同划分** —— FPGA 做加速、ARM 做数据控制；
  定点化降资源；再用**形态学后处理**补偿定点精度损失
- **开源**：代码已在 GitHub 开放

**对本作品的意义**（三条可直接对照的）：
1. **"FPGA 加速 + ARM 控制"的分工**与本作品 §3.1 一致
2. **定点化 + 后处理补偿精度**，与本作品"重排到 96×96 后靠 `gain`
   补偿 Sobel 响应偏弱"（§3.3）是同一思路
3. 它用**形态学后处理**，与本作品的 `morph_close` 对应

#### 本作品的差异点

| 维度 | 上述工作 | 本作品 |
|---|---|---|
| 边界 | **分层**：PL 做预处理加速 | **PL 全部**：采集→预处理→DDR 中继 |
| 与下游解耦 | CNN 在 PL 内（①②）或同板 PS（③） | CNN 在**另一侧**，只依赖 DDR 里 96×96 buffer，**可独立开发** |
| 算力取舍 | 尽量把 CNN 加速 | **明确不做 CNN 加速**，算力留给采集与预处理（§1.3①） |

> **一句话**：前人做的是"**把 CNN 放上去加速**"；
> 本作品做的是"**为 CNN 准备一份干净、低延迟的输入**"，
> 并把两侧边界收敛成**一个可验证的数据契约**。
---

## 二、设计原理与功能框图

### 2.1 系统总览

```
                    ┌──────────────── PYNQ-Z2 (XC7Z020) ────────────────┐
                    │                                                   │
  OV5640            │   PL (可编程逻辑)              PS (Cortex-A9)      │
  ┌────────┐        │  ┌──────────────────┐        ┌─────────────────┐ │
  │ 500万   │  DVP   │  │ 采集 → 灰度 →    │        │ PYNQ Linux      │ │
  │ 摄像头  ├───────►│  │ 高斯 → Sobel →   │  DDR   │  + Jupyter      │ │
  │        │ 14根线 │  │ 自适应阈值 →     ├───────►│                 │ │
  └────────┘        │  │ 形态学 → ROI     │  512MB │  ┌───────────┐  │ │
                    │  └──────────────────┘        │  │ 轻量 CNN  │  │ │
                    │         ▲                    │  │ (他人负责) │  │ │
  ┌────────┐        │         │ VDMA               │  └───────────┘  │ │
  │ 显示器  │◄───────┤  HDMI Out ◄─────────────────┤                 │ │
  └────────┘        │                              │  结果叠加显示    │ │
                    └──────────────────────────────┴─────────────────┘ │
                    └───────────────────────────────────────────────────┘
```

### 2.2 数据通路（两条，从 DDR 分叉）

```
【显示通路】摄像头原图直通，与预处理完全解耦
 dvp_capture ─AXIS(16bit)─► VDMA S2MM ─► HP1 ─► DDR(帧缓存,3帧)
                                                    │
                        VDMA MM2S ◄── HP2 ◄─────────┘
                             │
                             ▼
                     v_axi4s_vid_out ─► （并行视频；TMDS 编码器未做）

【CNN 通路】从 DDR 取同一帧
 DDR(帧缓存) ─► dma_in(MM2S) ─► gesture_preproc ─► dma_out(S2MM) ─► DDR(96×96)
                                                                        │
                                                              PS 侧 CNN 读这里
```

**为什么从 DDR 分叉，而不是从采集侧（`axis_broadcaster`）分叉**：

broadcaster 要求**所有输出都 ready 才接收输入**。
而 `gesture_preproc` 是 HLS 的 `ap_ctrl_hs` 模式 ——
**未 `ap_start` 时 `tready=0`**。CNN 不需要 30 fps，预处理大部分时间空闲，
于是 `gesture_preproc` 空闲 → broadcaster 反压 → VDMA 收不到数 →
**HDMI 显示也一起卡死**。这个反压是**常态化的，不是偶发**。

DDR 分叉的代价是多一次 DDR 往返（614 KB/帧读），三个 HP 口各自独立，占比很小。

### 2.3 时钟域

| 时钟 | 频率 | 来源 |
|---|---|---|
| `clk_fpga_0` | **100 MHz** | PS7 `FCLK_CLK0` |
| `fpga_pl_clk0` | 100 MHz | 同上 |
| `cam_pclk` | **24 MHz** | 摄像头 PCLK（外部输入） |
| `clk_wiz_xclk/clk_out1` | **24 MHz** | Clocking Wizard 输出 → `io_xclk` |

**跨时钟域**：摄像头 PCLK 域 → 系统 100 MHz 域，由 `async_fifo`
（**格雷码指针 + 双时钟**，标准 CDC 做法）处理。

> Clocking Wizard：100 → 24 MHz，**M=12 / D=1 / O=50，VCO = 1200 MHz**。
> **非整数分频，必须用 MMCM**（简单分频做不到）。

---

## 三、软硬件划分依据与接口设计

### 3.1 划分依据

**一条线**：**PL 做"每像素一次操作"的带宽型任务；PS 做"需要循环迭代与权重"的算力型任务。**

| 判据 | 用 Verilog | 用 HLS | 放 PS |
|---|---|---|---|
| 需要精确控制时钟域/边沿 | ✅ DVP 采集、SCCB 时序 | ❌ | ❌ |
| 需要例化原语（MMCM/IOBUF） | ✅ XCLK 生成、SDA 三态 | ❌ | ❌ |
| "每像素一次运算"的规整流水 | ❌ | ✅ 灰度/滤波/阈值/形态学 | ❌ |
| 需要频繁改参数调优 | ❌ | ✅ 阈值/增益/核大小 | ❌ |
| **需要循环迭代 + 权重的推理** | ❌ | ❌（占满 DSP） | ✅ **CNN** |

**具体分工**：

| 侧 | 承担 |
|---|---|
| **PL 全部**（本项目） | DVP 采集、SCCB 配置、图像预处理链、VDMA、HDMI 输出、DDR 中继 buffer |
| **PS**（他人） | 模型训练、量化、推理、后处理 |
| ~~PL 侧 CNN 加速~~ | **已评估否决**，理由见 §一.3.① |

### 3.2 接口契约（**唯一的一条边界**）

| 项 | 值 |
|---|---|
| **位置** | PS DDR，由 PL 侧的 `dma_out` 写入 |
| **格式** | `uint8` 灰度，行优先连续存放 |
| **尺寸** | **96 × 96**（9,216 字节） |
| **对齐** | 4 字节对齐（DMA 要求） |
| **取值范围** | 0–255，**不做归一化**（归一化由 CNN 侧负责） |
| **物理地址** | 由 PL 侧驱动分配后**写入寄存器**告知 CNN 侧，**不硬编码** |

**为什么这样定**（每条都有理由，不是随手定的）：

- **不归一化** → 归一化是模型侧的事，PL 不该替它做决定
- **地址不硬编码** → 让 CNN 侧可以在 PC 上独立开发调试，不受板子是否到货影响
- **裸 `.bin` 无头** → 加文件头就要两侧同步解析逻辑；裸二进制任何工具都能 `fread`

**握手寄存器**（轮询，暂不用中断）：

| 偏移 | 名称 | 方向 | 含义 |
|---|---|---|---|
| `0x00` | `CTRL` | 写 | bit0 = `capture_start` |
| `0x04` | `STATUS` | 读 | bit0 = `frame_ready`，bit1 = `busy` |
| `0x10` | `INPUT_ADDR` | 读/写 | 96×96 buffer 的物理地址 |
| `0x18` | `FRAME_SEQ` | 读 | 帧序号（丢帧检测用） |

> ⚠ **实现上注意**：HLS 生成的控制寄存器里，
> **`AP_DONE` / `AP_IDLE` 在 `CTRL`(0x00) 的 bit1–bit3**，
> **不在 `0x04`**（`0x04` 是 `GIER`）。
> 这是个经典误用，且**主机仿真发现不了**（仿真自己造了个假 STATUS 值）。

### 3.3 为什么预处理链是"重排后在 96×96 上跑"

预处理链的**标称输入是 640×480**，但**实际计算全部在 96×96 上进行**：

```
crop_scale（640×480 → 96×96）→ 高斯 → Sobel → 自适应阈值 → 闭运算
                              └──────── 全部在 96×96 上 ────────┘
```

**收益**：算力降到 1/33。**代价**：精度损失 —— 但手势识别不需要像素级精度。

> 这是**关键的设计取舍**：把算力花在"信息量最大"的尺度上。

---

## 四、优化过程

> 赛题 §3.3.5.5 把"性能与资源优化效果"列为 **20 分**，
> 且 §3.3.4 明确要求"给出**与基线的对比**"。
>
> **本节现在的完成度**（分开说，不含糊）：
>
> | 赛题要的 | 状态 |
> |---|---|
> | 资源/时序实测数据 | ✅ §4.1 |
> | 优化尝试与结论 | ✅ §4.2 / §4.3（**4 组对照实验 + 探针**）|
> | 时序瓶颈定位 | ✅ §4.5（**查出瓶颈不在本设计里**）|
> | **与基线的加速对比** | ❌ **§4.4 —— 唯一的真缺口，须等板卡** |
>
> **本节的特色是"负结果写全"**：§4.3 那次优化**没做成本身的改动**
> （代码一行没动），但把"为什么不做"用 4 组实测数据钉死了，
> 并且**明确区分了"找到了根因"与"能改得动"**这两件常被混为一谈的事。

### 4.1 已有的实测数据

#### 资源占用（PL 侧，实现后）

| 资源 | 用量 | 占比 | 说明 |
|---|---|---|---|
| Slice LUTs | **11,255** | **21.16%** | |
| Slice Registers | 14,810 | 13.92% | |
| **DSP48E1** | **61** | **27.73%** | ⚠ 主要来自 HLS 的 `morph_stage` 索引乘法（见 §4.2） |
| Block RAM Tile | 25.5 | 18.21% | |

> 数据取自 `../vivado/build-report.md`，与 `utilization.rpt` **逐项核对过**
> （2026-09-18 复核，四个数字一致）。
>
> ⚠ 旧版说明栏写的是"DSP 主要来自 HLS 的整数除法" —— **那是错的**，
> 且是本节**最早的一版错误**（后来还错过第二次，见 §4.2）。

#### HLS 预处理链（csynth 阶段）

| 资源 | 用量 | 占比 |
|---|---|---|
| LUT | **13,144** | 24% |
| FF | 11,177 | 10% |
| DSP | 71 | **32%** |
| BRAM | 34 | 12% |

**关键指标**：
- ✅ **所有循环 `II = 1`**（每周期出一个结果）
- ✅ **Est. Fmax = 143.31 MHz**（目标 100 MHz，**余量 +43%**）

> ⚠ 上表数字于 2026-09-18 从 `csynth.rpt` 的 `gesture_preproc` 行**逐字核对**。
> 旧版写的是 `13,184 / 137 MHz` —— **是凭记忆写的，已更正**。

#### 时序（实现后）

| 指标 | 值 |
|---|---|
| **WNS** | **+0.265 ns** |
| WHS | +0.051 ns |
| 失败端点 | 0 |
| 结论 | ✅ `All user specified timing constraints are met.` |

> ⚠ **WNS 逐次波动大**：同一设计三次实现为 **+0.873 / +1.177 / +0.265** ns。
> 布线是随机过程。详见 `../vivado/build-report.md` §3.1。
>
> ⚠⚠ **这个 `+0.265` 不是本设计的余量** —— 详见 §4.5。
> 它是 AMD `v_tc` IP 内部一条高扇出网的余量。

### 4.2 DSP 占用偏高，但**原因与原先的假设不符**

**实测各 stage 的 DSP 分布**（来自各模块的 `csynth.rpt`）：

| Stage | DSP | 说明 |
|---|---|---|
| **`morph_stage`** | **56** | **占全设计的 79%** |
| `crop_scale` | 4 | |
| `thresh_stage` | 2 | |
| `sobel_stage` | 1 | |
| `gaussian_stage` | 0 | |

**`morph_stage` 那 56 个再往下拆**（查 `csynth.rpt` 的 **Bind Op Report** 段）：

| 乘法器 | 位宽 | DSP |
|---|---|---:|
| `mul_64ns_66ns_129_5_1_U134/135/136` | **64×66→129** | **16 × 3 = 48** |
| 其余 8 个小算子 | 14×16→29 等 | 8 |

即 **68% 的 DSP 集中在 3 个宽乘法器上**，来自腐蚀流水线的下标
`r1[yy * width + xx]`（详见 §4.3）。

> ⚠ **本节更正过两次，都值得记**：
>
> **第一次**：原先写的是"DSP 来自 `thresh_stage` 的整数除法"——**错**，
> `thresh_stage` 只占 2 个。
>
> **第二次**：改对归属后，又写"`morph_stage` 的 DSP 来自若干带
> **16 位常数魔数**（如 21846）的乘法器"——**也错**。
> Bind Op Report 里根本没有 21846；真实是 **64 位**宽的乘法器。
>
> **教训**：DSP 的归属要**逐条查 Bind Op Report**，
> 不能凭"哪里有除法""哪里像魔数"推断。本项目在这个问题上
> 前后共 **3 条错误断言**，外加 **2 次对已证伪假设的重复尝试**，
> 最后由一个**固定值探针**才定案。

### 4.3 一次失败的资源优化尝试（有数据，负结果）

**动因**：`morph_stage` 占 56/71 个 DSP，想把它降下来。

**完整对照实验**（每次干净重跑，**TB 逐位比对通过**）：

| 改法 | **DSP** | **LUT** | FF | 语义 |
|---|---:|---:|---:|---|
| **基线**（当前，`width` 变量作步长） | **71** | **13,144** | **11,177** | ✅ |
| 步长 `width` → 编译期常量 `96` | 71 | 13,144 | 11,177 | ✅ |
| 9 个读全部显式展开 | **86** ↑ | 13,377 ↑ | 11,698 ↑ | ✅ |
| **探针**：索引换成固定值 | **15** ↓↓ | 10,235 | 8,123 | ❌ 故意破坏 |
| 模板参数化（旧记录） | 71 | — | — | ✅ |

**读法**：

- **探针（第 4 行）证明根因** —— 把 `r1[yy*width+xx]` 换成与循环变量无关的
  固定下标，DSP 从 71 掉到 **15**，那 3 个 64 位乘法器**全部消失**。
  **根因确凿：就是这段索引算术。**
- **但"改不掉"**（第 2、3 行）：
  - 步长改成编译期常量 → **毫无变化**。HLS 对归纳变量的范围推断不足，
    即便步长已知也不收窄位宽。
  - 手写展开 → **反而更差**（DSP +15、LUT +233）。HLS 会把 4 个乘法器
    一起摆上流水线。

**结论**：**找到了根因，但没有不损语义的改法。** 这条路径到此为止。

> ⚠ **负结果也是结果** —— 而且比"没考虑过"更有价值：它带着
> 一组可复现的数据说明"**为什么不能做**"。
>
> ⚠ 但**它仍不能替代赛题要的"性能提升对比"**，
> 见 §4.4。

> **顺带更正一条被推翻的旧结论**：本节曾记录"手写展开/边界分支会让
> II 从 1 退化成 2、关键路径变长会吃掉 `+0.265 ns` 时序余量"。
> 经查证**两处都不成立**：
> - 探针与①的 II 全程都是 **1**，从未退化到 2；
> - **`+0.265 ns` 根本不是本设计的余量** ——
>   全设计 10 条最差 setup 路径**全部属于 AMD 的 `bd_video_v_tc_0` IP
>   内部**（扇出 433 的网、布线 9.122 ns、逻辑仅 0.642 ns），
>   改我们的 RTL/HLS 对它零影响。详见 §4.5。

### 4.4 性能与基线对比（**加速比仍是 20 分缺口**）

| 待补项 | 做法 | 状态 |
|---|---|---|
| **PS 侧基线** | 在 PYNQ 上跑同一算法计时 | ❌ **仍未做 —— 但板已到，只差跑一次**（`host/bench_ps_baseline.py`） |
| HLS pragma 迭代前后 | 每轮记 `csynth.rpt` → `golden_compare.py report` 出表 | ✅ **已做**（见 `../src_hls/optimization-report.md`，结论是负结果但数据完整） |
| 上板实测吞吐 | 测实际帧率 / DMA 带宽 | ✅ **已做（2026-09-21）**：预处理单帧 **≈5 ms**；⚠ 摄像头通路未通，**端到端帧率仍未测** |

> ⚠ **注意"主机 CPU"不是"PS 基线"**。
> `host/bench_ps_baseline.py` 在 PC 上跑出约 **3.8 ms/帧**，
> 但那是 **Ryzen 9 8940HX**，而 PYNQ-Z2 的 PS 是 **Cortex-A9 @650MHz** ——
> 差着代际，**不能拿来当基线**。该脚本已明确标注为"主机参考值"，
> **可直接拷到 PYNQ 上复用**，那才是报告要的数。

**PL 侧的延迟（csynth 加总估算 → 上板实测已更正）**：

| Stage | 延迟 (cycles) | @100MHz |
|---|---|---|
| **`crop_scale`** | **307,200** | **3.07 ms** ← ⚠ 见下方更正 |
| `thresh_stage` | 18,443 | 0.184 ms |
| `gaussian_stage` | 9,821 | 0.098 ms |
| `sobel_stage` | 9,822 | 0.098 ms |
| `morph_stage` | 19,066 | 0.191 ms |
| `output_stage` | 9,218 | 0.092 ms |
| **合计** | **≈ 373,000** | **≈ 3.7 ms** |
| **上板实测（2026-09-21）** | — | **≈ 5 ms/帧**（含 DMA 建立与轮询开销） |

> ⚠⚠ **更正（2026-09-21 首次上板实测）**：本表原先给 `crop_scale` 记的是
> **2,073,634 拍 / 20.74 ms**，并据此断言"整链 ≈21.4 ms、crop_scale 占 97%"。
> **那个数是错的。**
>
> `2,073,634` 是 `LOOP_TRIPCOUNT max` 的**循环上界乘积**
> （`480/1080 × 640/1920` 的 max 情形），**不是实际执行次数**。
> 而实现的 `crop_scale` 内层循环是 `II=1`、**每拍读一个输入像素**
> （`src_hls/gesture_preproc.cpp:207` 的 `PIPELINE II=1`），
> 逐行扫满 `width` —— 真实拍数就是 **640×480 = 307,200**。
>
> **上板实测佐证**：整链单帧 **≈5 ms**，与"3.07 ms + 下游 0.66 ms + 开销"
> 完全吻合；若真是 20.74 ms，实测不可能是 5 ms。
>
> **教训**：`LOOP_TRIPCOUNT` 是**给工具做面积/时序估计用的上界**，
> 不是运行时的实际迭代数。**引用它之前，先确认它与实现里的循环边界一致。**
> （同 `vivado/README.md` 坑 20 的教训：BD 默认参数 ≠ 实际行为。）

> ✅ **`crop_scale` 仍然是最大的一级**（占 ~82%），其余五级只占 ~18%。
> 它天生要碰每一个输入像素，**这部分延迟是结构性下限，不是优化空间**。
> 但**它不是"占 97% 的绝对瓶颈"** —— 原先的结论被夸大了一个数量级。

#### 为什么 `crop_scale` 是最慢的一级

它的内层循环要**逐像素读满整帧 640×480 = 307,200 次**（每拍一个像素），
而其余五级都只跑 9,216 个像素。**3.07 ms @100 MHz**
在 30 fps（33 ms/帧）的预算里占了约 **9%**。

> 这正是 §3.3"重排到 96×96 上算"的**另一半代价**：
> 重排让滤波器省了 33× 算力，但**采集/缩放这一级无法重排** ——
> 它天生要碰每一个输入像素。**这部分延迟是结构性下限，不是优化空间。**

> ⚠ 上表前六行的**逐级**数字仍是从 `preproc_pipeline` 的 instance 表加总
> 得到的估算（顶层 `DATAFLOW` 的 latency 在 csynth 里显示为 `?`，
> 各 stage 重叠执行，HLS 给不出静态总延迟）。
> **权威数字是上板实测的 ≈5 ms/帧**，其口径见
> `../docs/board-test-log-2026-09-21.md`（DDR→预处理→DDR，**不接摄像头**）。

> **诚实说明**：本节**仍不足以支撑 20 分**。已有的只是
> "单点资源/时序数据 + 上板实测单帧耗时 + 一次负结果的优化尝试"，
> **缺的是"与 PS 基线的加速比"** —— 那要在 PYNQ 的 Cortex-A9 上测，
> 见 `README.md` 的 P1。

---

### 4.5 时序分析：最差路径**不在本设计里**

> 这一节是 2026-09-18 查布线后时序报告得出的，
> **推翻了"我们的设计时序很紧"这个一直以来的印象**。

#### 事实

查 `bd_video_wrapper_timing_summary_routed.rpt`：

**全设计 10 条最差 setup 路径，全部属于 `bd_video_v_tc_0`（AMD 的视频时序控制器 IP）**：

```
Slack (MET) :   0.265ns
  Source / Target :  .../bd_video_v_tc_0/<hidden>/<hidden>/<hidden>   ← 加密 IP 内部
  Path Group      :  clk_fpga_0
  Logic Levels    :  1  (LUT3=1)
  Data Path Delay :  9.764ns  (logic 0.642ns (6.6%)  route 9.122ns (93.4%))
  net (fo=433, routed)  9.122ns      ← 扇出 433 的高扇出网
  Timing Exception:  MaxDelay Path 10.000ns -datapath_only
```

| 事实 | 含义 |
|---|---|
| 逻辑仅 **0.642 ns**、布线 **9.122 ns** | **不是逻辑深度问题**，是布线绕远了 |
| 两端都在 `<hidden>` 的加密 IP **内部** | **改我们的 RTL/HLS 对它零影响** |
| 扇出 **433** | 典型的"高扇出网布线主导" |

#### 与我们自己的流水线对比

| | 目标 | 估计值 | 余量 |
|---|---|---|---|
| `morph_stage` 腐蚀流水线（csynth） | 100 MHz | **143.31 MHz** | **+43%** |
| `clk_fpga_0` 整体（布线后） | 100 MHz | — | +2.65%（**v_tc**） |

HLS 估 143 MHz、整体只有 100 MHz —— **差 30%**。
差异不在 HLS 估错，而在**整体最差路径是 IP 的布线，不是我们的流水线**。

#### 结论：**不优化，如实标注**

| 想法 | 判断 |
|---|---|
| 优化 HLS 流水线 | ❌ 它不是关键路径（余量 43%） |
| 优化我们自己的 RTL | ❌ 10 条最差路径没有一条属于 `dvp_capture` / `gesture_preproc` |
| 去掉 `-datapath_only` 例外 | ❌ 数字会"变好看"，但**那是粉饰**，不做 |
| 给该扇出网加 `MAX_FANOUT` | ❌ 加密 IP 内部网，**约束不进去** |
| 降 `clk_fpga_0` 到 80 MHz | ❌ 违反性能目标（见下方说明） |

> **关于"降频"这一条**：此处原先写的是"28 → 22 FPS"。
> **那两个数没有任何实测或推算支撑**，已删。
>
> 按 §4.4 的**实测**，PL 侧单帧约 **5 ms @100 MHz**，其中
> `crop_scale` 占 ~82%。若降到 80 MHz，**`crop_scale` 按周期等比换算**
> 会变成约 25.9 ms，整链随之上升 —— 但**其余各级能否被 `DATAFLOW`
> 掩盖、DMA 与 DDR 带宽贡献多少，仍没有拆分实测**（已上板，但未做带宽专项测量）。
> 所以**帧率只能等上板测**，现在给任何具体 FPS 都是编的。
>
> 这条的实测口径见 §4.4："上板实测吞吐"**已做**（预处理单帧 ≈5 ms），
> 但**端到端帧率（含摄像头）仍未测**。

理由：**它在 AMD 官方 IP 内部，是 IP 可用性问题而非本作品的性能瓶颈**。
且**换布线的随机波动比它大**（同一设计三次实现 `+0.873 / +1.177 / +0.265`），
任何"优化"都是在 IP 外面猜 IP 里面的布线。

> 完整的实验数据与复现方式见 [`../src_hls/optimization-report.md`](../src_hls/optimization-report.md)。

---

## 五、大模型协作记录

→ **见 [`llm-collaboration.md`](llm-collaboration.md)**

摘要：本项目的**多次结论先被写进文档、后被实测推翻** ——
那篇记录了 4 个可复用的误判模式、对话原话摘录，
以及"哪些做法被实测证明有效 / 无效 / 未验证"的三分表。

---

## 六、技能包的提炼过程

→ **见 [`../skill/README.md`](../skill/README.md)**

**提炼标准**：**"这个内容对做别的题目的队伍有用吗？"**
有用 → `skill/`；只是本项目的记录 → `docs/`。

| 产出 | 通用性 |
|---|---|
| 4 个 PYNQ 校验工具（`skill/pynq/`） | **换题目、换板卡仍可用**（赛题 §3.3.2 的单独加分项） |
| 19 条踩坑，含触发条件与排查步骤（`skill/pitfalls/`） | 与题目无关 |
| 纠错方法论 4 模式 + 自检清单（`skill/self-correction/`） | 与题目无关 |

**这些不是"事后整理"的** —— 它们是开发过程中**真实踩坑后立刻记录**的。
赛题 §3.3.5.2 要求说明"从哪些失败或观察中总结"，
每一条的出处都能在 `../vivado/README.md` 或对话记录里找到。

---

## 七、复现说明

→ **见项目根 [`README.md`](../README.md) 的「从零构建完整工程」一节**

三步：

```bash
# ① 必须先跑 HLS，产出 IP 仓库（create_project.tcl 依赖它）
vitis-run --mode hls --tcl src_hls/run_gesture.tcl

# ② 建工程 → BD → 综合 → 实现 → 比特流 → XSA（约 20–40 分钟）
vivado -mode batch -source vivado/create_project.tcl

# ③ 只想验证 BD 是否合法（不跑综合，约 1 分钟）
vivado -mode batch -source vivado/create_project.tcl -tclargs --synth 0
```

**目录对照说明**（赛题推荐结构与本仓库的映射）→ 见
[`../docs/README.md`](../docs/README.md)。

---

## ⚠ 提交前的已知缺口（如实列出）

| 项 | 状态 | 影响 |
|---|---|---|
| **§4.4 加速比对比** | ❌ **须在 PYNQ 的 Cortex-A9 上测**；板已到，**只差跑一次** | **20 分项中唯一未完成的部分** |
| **板级实测** | ⚠ **部分已做（2026-09-21）**：②③ 预处理链 11/11 全过、单帧 ≈5 ms。**④~⑧ 摄像头通路未通**，**端到端图像实测输出仍缺** | 赛题要求"实测输出"——**这一条仍未完全满足** |
| OV5640 出图验证 | ⚠ 已换真表，上板确认事务有发出但**未收到 ACK**（`cfg_error=1`），**未出图** | 功能正确性 |
| `legacy/` 下 1 个中文文件名 | ⚠ 赛题要求纯英文（已在文档说明理由） | 形式合规 |

> 已消掉的缺口：
> - **§一.4 学术引用** —— 已补 3 篇**核验过 DOI** 的文献（§1.4）
> - **§四 优化对照** —— 已补 4 组对照实验 + 时序分析（§4.2/4.3/4.5，
>   详见 [`../src_hls/optimization-report.md`](../src_hls/optimization-report.md)）
> - **上板实测（②③）** —— 2026-09-21 完成，含一个全流程静默通过的 DMA bug
>   （见 [`../docs/board-test-log-2026-09-21.md`](../docs/board-test-log-2026-09-21.md)）
> - **§4.4 `crop_scale` 延迟** —— 已由上板实测更正（原 20.74 ms 是
>   `LOOP_TRIPCOUNT` 上界误当实际拍数，实为 3.07 ms）
> - **§2.2 的推论**"把 CNN 放 PL 会顶到供电/散热上限" —— 已找到
>   同板同场景的功耗实测（2.136 W）佐证（§1.4①）
