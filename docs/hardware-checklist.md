# 手势识别项目 —— 硬件采购清单

> 项目：基于 PYNQ-Z2 (XC7Z020) 的 PL 侧手势图像预处理 + PS 侧 CNN 识别
> 日期：2026-09-14
> **硬约束：不使用杜邦飞线连接任何高速信号。**
>
> 理由：DVP 的 `PCLK` 是 24 MHz 时钟，与 8 位数据线并行传输。
> 实测与社区案例一致表明，跨排针的杜邦线在 >30 MHz 时会出现
> 时钟/数据错位（花屏、丢行、偶发错位），且接触电阻不稳定会让
> 问题表现为"偶尔正常"，极难定位。本题的引脚核对与信号完整性
> 是本项目最大的物理风险点，不能在第一步就引入不可控变量。

---

## 一、必买（核心路径）

### 1.1 主控板

| # | 物品 | 规格 | 数量 | 预估 | 备注 |
|---|---|---|---|---|---|
| 1 | **PYNQ-Z2 开发板** | TUL 原厂，XC7Z020-1CLG400C | 1 | ¥1000–1300 | 本机已有资料（`Desktop\prepare\PYNQ-Z2资料`）。**若已持有请跳过** |
| 2 | MicroSD 卡 | 16 GB Class10 / A1，**另备 1 张** | 2 | ¥25×2 | 刷 PYNQ 镜像（v3.0.1）。备用卡用于镜像刷坏时回滚 |
| 3 | Micro-USB 线 | 数据线，非纯充电线 | 2 | ¥10×2 | J8 供电 + UART/JTAG |

> **供电警告**：OV5640 启动与全分辨率工作时峰值电流可超 200 mA。
> 板上 Micro-USB (J8) 供电在同时驱动 HDMI + 摄像头时可能不足，
> **建议同时采购 DC 电源**（见 1.4），并注意板上跳线 J9：
> `USB` 档 = Micro-USB 供电；`REG` 档 = 外部电源/电池。
> **用外部电源却把 J9 留在 USB 档，板子不会正常启动。**

### 1.2 摄像头（方案已定：PMOD-CAMERA v1.0）

| # | 物品 | 规格 | 数量 | 预估 | 备注 |
|---|---|---|---|---|---|
| 4 | **MUSE LAB PMOD-CAMERA v1.0** | **标准 Pmod 双口**，OV5640，含镜头 | 1 | — | **直插，零焊接** |
| 5 | 备选：正点原子 ATK-OV5640 | 2×9 (2.54mm) 排针 | 1 | ¥100–150 | 仅在前者缺货/引脚不符时考虑，需自制 Arduino 转接板 |

#### 为什么选 PMOD-CAMERA v1.0（2026-09-15 决策，推翻了 ATK 方案）

| | ATK-OV5640 | **PMOD-CAMERA v1.0** |
|---|---|---|
| 接口 | 2×9 排针 | **标准 Pmod 2×6 ×2** |
| **焊接** | 要焊 Arduino 转接板 | **零 —— 公头直插母座** ★ |
| PYNQ-Z2 验证 | 无 | **有现成开源项目** ★ |
| 布线 | 全在 Arduino 一个口 | 跨 Pmod A + B |
| XCLK | 模块自带晶振 | **PL 要产生**（多一个 MMCM） |
| 信号数 | 13 | 14 |

前两行是决定性的：**零焊接 + 有 PYNQ-Z2 跑通的先例**。
（开源项目 `xupsh/Pynq-CV-OV5640` 的硬件清单里明确列了
"PMOD-Camera adapter"，与本模块同名。）

代价是占两个 Pmod 口，且 PL 里要多一个 Clocking Wizard 产生 24 MHz XCLK。

#### ✅ 引脚编号：**标准 Pmod 编号**（2026-09-22 实测更正）

> ## ⚠⚠ 本节原写"引脚编号是镜像的"，**那个判断是错的**
>
> **实测方法：直接读模块丝印**（PMOD A / PMOD B 两个连接器，
> 每针都印了信号名）。丝印给出的电源脚是：
>
> ```
> 5 = GND   6 = 3V3   11 = GND   12 = 3V3
> ```
>
> **这正是 Pmod 规范的标准位置**（第 5/6 列是电源）。
> 而"镜像"解读会把电源推到 5/6/7/8 —— **两者不相容**。
> 既然丝印符合标准规范，模块用的就是**标准编号**。
>
> **代价**：按镜像推导的 `video_io.xdc` 让 **14 个信号全部接错**
> （Pmod A 的 6 个 + Pmod B 的 8 个），这是"摄像头不出图"
> 排查一个月的**真正根因**。已按丝印更正。
>
> **一个反直觉之处**：模块丝印上的信号名（`XMCLK`/`DVP_VSYNC`/
> `I2C_SDA`…）**与原理图一致**，错的是**针号解读方式** ——
> 原理图的 J2/J3 用了非标准的画法（左列 12→7），
> 但丝印忠于标准 Pmod 编号，两者**不能混用**。
>
> **教训**：**丝印 > 原理图推理**。丝印是实物的直接证据，
> 而"从原理图画法推断针号"引入了额外假设。
> 见 `skill/pitfalls/README.md` **P12**。

### 1.3 连接方案：直插，无需转接

**PMOD-CAMERA 是公头，PYNQ-Z2 的 Pmod 口是母座 —— 直接插上即可。**

| # | 物品 | 规格 | 数量 | 预估 |
|---|---|---|---|---|
| 6 | ~~洞洞板~~ | — | — | **不需要** |
| 7 | ~~排针排母~~ | — | — | **不需要** |
| 8 | ~~焊接工具~~ | — | — | **不需要** |

> 原来为"自制 Arduino 转接板"列的 4 项（洞洞板、排母、漆包线、
> 焊接工具）**全部取消**。这是换方案后省掉的最大一块工作量。

#### 仍需注意的三件事

1. **Pmod A 与 Raspberry Pi 口 8 根线并联，二者只能用其一**
   （8 根是 Y18 Y19 Y16 Y17 U18 U19 W18 W19）。
   本方案用 Pmod A + B，**RPi 口同时废掉** —— 本项目不用 RPi，可接受。
   好消息：SCCB 落在 Pmod A 的普通 GPIO 上（不是 RPi 口那 8 根），
   所以可以直接用已写好的 `rtl/sccb_master.v`，不需要 AXI IIC IP。

2. **XCLK 必须由 PL 产生**
   `J2 pin 1 = XMCLK` 是**输入**，要 FPGA 给它 24 MHz。
   100 MHz → 24 MHz 需要 MMCM（不是整数分频），
   参数 M=12 / D=1 / O=50，VCO = 1200 MHz。
   这是 **BD 的新增模块**（Clocking Wizard），不是约束能解决的。

3. **降低 PCLK 的备选手段**（若实测有采样错误再加）
   OV5640 的 PCLK 由内部 PLL 分频（寄存器 `0x3824`）。
   24 MHz → 12 MHz 可放大数据建立/保持裕量，帧率 30→15fps，
   对 96×96 手势识别完全够用（CNN 侧 10fps 都行）。
   **属于调试旋钮，不是采购项。**


### 1.4 供电

| # | 物品 | 规格 | 数量 | 预估 | 备注 |
|---|---|---|---|---|---|
| 11 | DC 电源适配器 | **12V / 2A**，2.1 mm 圆孔，中心正极 | 1 | ¥25 | 板上 DC1，支持 7–15 VDC |
| 12 | 电源转接线 | 若需接 Arduino 排针的 VIN | 1 | ¥5 | 配 J9 拨到 `REG` |

### 1.5 输出与验证

| # | 物品 | 规格 | 数量 | 预估 | 备注 |
|---|---|---|---|---|---|
| 13 | HDMI 线 | 标准 HDMI 公对公 | 1 | ¥15 | HDMI Out 看 720p 结果 |
| 14 | 显示器 | 支持 720p 即可 | 1 | 视情况 | 演示必需件 |

> **HDMI 规格提醒**：PYNQ-Z2 的 HDMI **直连 PL 的 TMDS 引脚**，
> 板载无 ADV7511 等编码芯片。官方推荐 **720p**；
> 1080p 接近带宽极限，非官方保证。

---

## 二、可选（按需增补）

| # | 物品 | 用途 | 必要性 | 预估 |
|---|---|---|---|---|
| 15 | HDMI 信号源（笔记本/手机转 HDMI） | 备选输入通道，替代摄像头 | 建议 | — |
| 16 | 二轴舵机云台 + 舵机 | 摄像头跟随手势转动，**竞赛创新点** | 竞赛建议 | ¥60–120 |
| 17 | 舵机驱动板 / 外部 5V 供电 | 舵机电流大，**不能从板卡取电** | 同上 | ¥20 |
| 18 | 散热片 | Zynq 在高负载下发热 | 可选 | ¥10 |
| 19 | 逻辑分析仪（8 通道） | 调试 DVP 时序的**决定性工具** | **强烈建议** | ¥40–80 |
| 20 | 万用表 | 上电前查短路、查电平 | 建议 | ¥30 |
| 21 | 亚克力支架 / 外壳 | 演示时固定 | 可选 | ¥30 |

> **逻辑分析仪的优先级被低估了**：DVP 采集调不通时，唯一能直接
> 看到"HREF 有没有、PCLK 沿对不对、数据线是不是恒 0"的手段就是它。
> 没有它，只能靠猜和改代码，调试周期会从半天变成一周。

---
## 三、引脚分配表（PMOD-CAMERA v1.0 直插）

### 3.1 可用引脚与占用

| 接口 | 可用 IO | 本方案占用 |
|---|---|---|
| **Pmod A** (`ja[0..7]`) | 8 | **6**（XCLK/VSYNC/SDA/HREF/SCL/PCLK） |
| **Pmod B** (`jb[0..7]`) | 8 | **8**（D0–D7） |
| Pmod A 余量 | — | 2（模块的 NC 脚，未用） |

> ⚠ **Pmod A 与 Raspberry Pi 口 8 根线并联，二者只能用其一**
> （Y18 Y19 Y16 Y17 U18 U19 W18 W19）。本方案用 Pmod A，
> **RPi 口同时废掉** —— 本项目不用 RPi，可接受。

### 3.2 实际映射（模块连接器 → PYNQ 端口）

**J2 → Pmod A**

| 模块信号 | PYNQ 端口 | FPGA 引脚 | 方向 |
|---|---|---|---|
| `XMCLK` | `ja[0]` | Y18 | **FPGA 输出**（24 MHz） |
| `DVP_VSYNC` | `ja[1]` | Y19 | 输入 |
| `I2C_SDA` | `ja[2]` | Y16 | **双向** |
| NC | `ja[3]` | Y17 | — |
| `DVP_PCLK` | `ja[4]` | U18 | 输入 |
| `DVP_HREF` | `ja[5]` | U19 | 输入 |
| `I2C_SCL` | `ja[6]` | W18 | 输出 |
| NC | `ja[7]` | W19 | — |

**J3 → Pmod B**

| 模块信号 | PYNQ 端口 | FPGA 引脚 |
|---|---|---|
| `D6` | `jb[0]` | W14 |
| `D4` | `jb[1]` | Y14 |
| `D2` | `jb[2]` | T11 |
| `D0` | `jb[3]` | T10 |
| `D7` | `jb[4]` | V16 |
| `D5` | `jb[5]` | W16 |
| `D3` | `jb[6]` | V12 |
| `D1` | `jb[7]` | W13 |

> ⚠ **数据线在模块上是交错的**（不是 D0..D7 顺序排）。
> 上表是原理图的实际连接，不是简化。

**完整约束在 `vivado/constraints/video_io.xdc` 第三层。**

### 3.3 上电前必查清单

- [ ] **万用表确认 3V3 / GND 的物理列**（本方案的关键风险，见 §1.2）
- [ ] 万用表量模块 **3V3 与 GND 不短路**
- [ ] 确认模块 IO 电平是 **3.3V**（PYNQ-Z2 无 5V 容忍）
- [ ] 插接**方向正确**（公头对齐母座的防呆缺口）
- [ ] 板上 J9 跳线与实际供电方式一致
- [ ] 摄像头镜头**保护膜已撕**

---

## 四、采购优先级

| 优先级 | 物品 | 理由 |
|---|---|---|
| **P0** | PYNQ-Z2、SD 卡、Micro-USB、12V 电源 | 没有这些什么都做不了 |
| **P0** | **MUSE LAB PMOD-CAMERA v1.0** | 项目主线 |
| **P0** | HDMI 线 + 显示器 | 结果验证必需 |
| **P1** | **逻辑分析仪** | 调试 DVP 的决定性工具，建议提前买 |
| **P1** | 万用表 | **本方案必买** —— 引脚核对靠它 |
| **P2** | 舵机云台、散热片、外壳 | 竞赛创新点与演示效果 |
| ~~取消~~ | ~~洞洞板 / 排针排母 / 焊接工具~~ | 直插方案已不需要 |

---

## 五、已知风险与规避

| 风险 | 后果 | 规避 |
|---|---|---|
| **引脚编号镜像解读错误** | **3V3/GND 反接，烧板** | **上电前万用表复核物理列**（§1.2 与 XDC 末尾） |
| 插接方向反 | 同上 | 对齐公母防呆缺口；插前拍照记录 |
| PCLK 与数据错位 | 花屏、丢行 | 降 PCLK 到 12 MHz（寄存器 `0x3824`） |
| XCLK 未产生 | 摄像头完全无输出 | 先确认 io_xclk 有 24 MHz，再看其他 |
| SCCB 配置失败 | PCLK 一直无波形 | 查 `sccb_master.cfg_error`；确认寄存器表内容 |
| 供电不足 | 摄像头初始化失败、随机重启 | 用 12V DC 供电，J9 拨 `REG` |

> **排查顺序建议**（DVP 无图像时的最快路径）：
>
> 1. 示波器 / ILA 看 `io_xclk` 有没有 24 MHz
> 2. 看 `io_scl` 有没有在跑（说明 SCCB 在工作）
> 3. 看 `io_pclk` 有没有波形 —— **有 = 配置成功，无 = 配置失败**
> 4. 再看 `io_href` / `io_vsync` 有没有周期性脉冲
> 5. 最后才查数据线
>
> 前两步能快速区分"FPGA 侧没工作"和"摄像头没配上"。

---

## 六、数据来源

- 引脚与电气约束：`PYNQ-Z2资料\PYNQ-Z2_v1.0_master.xdc`（TUL 原厂）
- 板卡规格与共享引脚说明：同目录 `PYNQ-Z2资料汇编.md`、`PYNQ-Z2_User_Manual_v1.0.pdf`
- **摄像头引脚**：模块原理图 `pmod-camera-v1.0.pdf`
  （用 PyMuPDF 提取文字层 + 按坐标重建连接器布局，非视觉 OCR）
- OV5640 DVP 时序与典型接线：社区公开案例
- 开源参考：`xupsh/Pynq-CV-OV5640`（PYNQ-Z2 + 同名 adapter 的跑通项目）
- 价格区间为 2026-09 国内市场常见零售价，仅供预算参考
